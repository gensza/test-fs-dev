Berikut penjelasan untuk melihat hasil jawaban dari soal test yang diberikan dalam bentuk endpoint:

1. DB ada di dalam folder dengan nama apbatech.sql
   
2. endpoint:
   (POST) api/users/posts
   (PUT) api/users/update
   (DELETE) api/users/delete
   (GET) api/users
   
3. endpoint:
   (POST) api/auth/login
   (POST) api/auth/decrypt
   
4. endpoint
   (GET) api/test/no-4-array
   
5. endpoint
   (GET) api/test/no-5-string

6. endpoint
   (GET) api/test/no-6-terbilang

Terimakasih
